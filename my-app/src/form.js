import React from "react";

function AddItem() {

    const handleSubmit=({})
  return (
    <div>
      <input ></input>
      <button onClick={handleSubmit}>Submit</button>
      <ul></ul>
      <button>done</button>
      <button>edit</button>
      <button>edit</button>
    </div>
  );
}

export default AddItem